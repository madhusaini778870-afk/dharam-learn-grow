{
  "previewPort": 3000,
  "healthPath": "/",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "SUPABASE_URL",
      "description": "Supabase project URL (https://xxxxx.supabase.co). Required for auth and database.",
      "requiredAtBoot": true
    },
    {
      "name": "SUPABASE_PUBLISHABLE_KEY",
      "description": "Supabase publishable/anon key. Required for client-side auth.",
      "requiredAtBoot": true
    },
    {
      "name": "SUPABASE_SERVICE_ROLE_KEY",
      "description": "Supabase service role key. Used server-side for enrollments and progress.",
      "requiredAtBoot": true
    },
    {
      "name": "LOVABLE_API_KEY",
      "description": "Lovable AI gateway key for the AI Doubt Solver. Optional for boot.",
      "requiredAtBoot": false
    }
  ]
}
