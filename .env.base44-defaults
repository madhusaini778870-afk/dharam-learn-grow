# Development placeholders so the app boots without real credentials.
# Real values are delivered via /run/base44/app.env (platform secrets) and
# override these because that file is listed last in compose env_file.
SUPABASE_URL=https://placeholder.supabase.co
SUPABASE_PUBLISHABLE_KEY=sb_publishable_placeholder
SUPABASE_SERVICE_ROLE_KEY=sb_secret_placeholder
LOVABLE_API_KEY=placeholder
